apiVersion: v1
kind: Service
metadata:
  labels:
    project: admin
  name: adminer
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    project: admin
status:
  loadBalancer: {}
